#!/bin/bash
kill -0 emacs >&/dev/null || emacs --daemon >&/dev/null || notify-send EW 'emacs failed to start'
EMACSCLIENT_ARGS=()
[ -z "$INSIDE_EMACS" ] && EMACSCLIENT_ARGS+=(-c)
exec nohup emacsclient "${EMACSCLIENT_ARGS[@]}" "$@" >& /dev/null &
