#!/bin/bash
emacs --daemon -q --eval "(setq use-exwm t)" -l "$HOME/.emacs"
#"(require 'exwm)" --eval "(require 'exwm-config)" --eval "(exwm-config-default)" --eval "(setq mouse-autoselect-window nil)"
exec emacsclient -a "" -c
